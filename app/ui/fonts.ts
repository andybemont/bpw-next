import { Lato as title, Lato as main } from "next/font/google";

export const titleText = title({ weight: "400", subsets: ["latin"] });
export const mainText = main({ weight: "400", subsets: ["latin"] });
